<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.expensetracker.MainActivity" >

    <ListView
        android:id="@+id/ClaimsListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/AddClaimButton"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true" >

    </ListView>

    <Button
        android:id="@+id/AddClaimButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/ClaimsListView"
        android:layout_marginBottom="18dp"
        android:onClick="onClickAddClaimButton"
        android:text="@string/add_claim" />

    <Button
        android:id="@+id/checkExpenseItems"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/AddClaimButton"
        android:layout_alignBottom="@+id/AddClaimButton"
        android:layout_alignLeft="@+id/ClaimsListView"
        android:onClick="onClickCheckExpenseItemButton"
        android:text="Check Expense" />

</RelativeLayout>
